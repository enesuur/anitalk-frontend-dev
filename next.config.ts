/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'cdn.jsdelivr.net',
      'picsum.photos',
      'avatars.githubusercontent.com',
      'via.placeholder.com',
    ],
  },
};

export default nextConfig;
